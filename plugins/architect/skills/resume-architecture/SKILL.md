---
name: resume-architecture
description: Resume Architect work from existing design and remote BA readiness.
---

# Resume Architecture

`praxis doctor --json` then `praxis architect preview --epic <EPIC> --repo . --json`. Apply only with `--confirm YES`. If CLI cannot run: `LOCAL_RUNTIME_UNAVAILABLE`.
